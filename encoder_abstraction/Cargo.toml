[package]
name = "encoder_abstraction"
version = "0.1.0"
edition = "2024"

[dependencies]
nvenc = "0.1"
widestring = "1.2.0"
amffi = { version = "0.0.2", default-features = false }

[target.'cfg(windows)'.dependencies.windows]
version = "0.62"
features = [
    "Win32_Graphics_Dxgi_Common",
    "Win32_Graphics_Direct3D",
    "Win32_Graphics_Direct3D11",
]

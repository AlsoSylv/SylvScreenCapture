[package]
name = "screen-recorder"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["dylib"]

[dependencies]
windows.workspace = true
# interprocess.workspace = true
# shared_memory.workspace = true
glad-gl.workspace = true
shmem.workspace = true
vulkanalia = { version = "0.25.0", features = ["libloading", "window"] }
vulkanalia-vma = "0.2.0"
raw-window-handle = "0.6.2"
retour = { version = "0.3.1" }
